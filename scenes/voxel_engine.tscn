[gd_scene load_steps=13 format=3 uid="uid://c5veq388pr3ei"]

[ext_resource type="Texture2D" uid="uid://r10b1gjkahn8" path="res://assets/selection.png" id="3_1ynvo"]
[ext_resource type="Script" uid="uid://dkehu32hlwjtg" path="res://scripts/player.gd" id="3_8ls5l"]

[sub_resource type="GDScript" id="GDScript_w4k74"]
script/source = "extends Node3D


func _process(_delta: float) -> void:
	$FPSLabel.text = \"FPS : \" + str(Engine.get_frames_per_second())
	$TrianglesLabel.text = \"Triangles : \" + str($ChunkManager.triangles_total)
"

[sub_resource type="ProceduralSkyMaterial" id="ProceduralSkyMaterial_ps217"]
sky_horizon_color = Color(0.662243, 0.671743, 0.686743, 1)
ground_horizon_color = Color(0.662243, 0.671743, 0.686743, 1)

[sub_resource type="Sky" id="Sky_w4k74"]
sky_material = SubResource("ProceduralSkyMaterial_ps217")

[sub_resource type="Environment" id="Environment_8ls5l"]
background_mode = 2
sky = SubResource("Sky_w4k74")
tonemap_mode = 2
glow_enabled = true

[sub_resource type="GDScript" id="GDScript_8ls5l"]
script/source = "extends Node


@export var chunk_size := 16
@export var max_height := 16
@export var noise_scale := 3
@export var world_size := 4

@onready var chunk_scene = preload(\"res://scenes/chunk.tscn\")

var _chunk_triangles: Dictionary = {} # key: Vector2i(chunk_x, chunk_z) -> int
var _chunks: Dictionary = {} # key: Vector2i(chunk_x, chunk_z) -> int
var triangles_total: int


func _ready() -> void:
	var sn = FastNoiseLite.new()
	sn.noise_type = FastNoiseLite.TYPE_PERLIN
	sn.seed = 20140114
	
	for x in range(world_size):
		for z in range(world_size):
			var key := Vector2i(x, z)
			
			var chunk = chunk_scene.instantiate()
			add_child(chunk)
			
			chunk.key = key
			chunk.chunk_offset = Vector3(x * chunk_size, 0, z * chunk_size)
			chunk.chunk_manager = self
			#chunk.chunk_color = Color.WHITE if (x + z) % 2 == 0 else Color.BLUE
			
			chunk.init_mesh(sn)
			chunk.build_mesh()
			
			_register_chunk(chunk, key)


func _register_chunk(chunk, key) -> void:
	_chunks[key] = chunk
	
	chunk.mesh_updated.connect(_on_chunk_mesh_updated)
	chunk.border_update_requested.connect(_on_border_update_requested)
	
	# rebuild neighbours
	var dirs = [
		Vector2i(1, 0),
		Vector2i(-1, 0), 
		Vector2i(0, 1),
		Vector2i(0, -1)
	]
	
	await get_tree().process_frame
	
	for d in dirs:
		var nk = key + d
		if _chunks.has(nk):
			_chunks[nk].build_mesh()
			
	await get_tree().process_frame
	# also rebuild self
	chunk.build_mesh()
	
	
func is_air_world(wx: int, wy: int, wz: int) -> bool:
	if wx < 0 or wy < 0 or wz < 0 or wx >= chunk_size * world_size or wy >= chunk_size or wz >= chunk_size * world_size:
		return true # outside world
		
	# check chunk coordinates
	var cx := wx / chunk_size
	var cz := wz / chunk_size
	var key := Vector2i(cx, cz)
	
	if not _chunks.has(key):
		return true # no chunk
		
	var chunk = _chunks[key]
	
	# check local chunk coordinates
	var lx := wx - cx * chunk_size
	var ly := wy
	var lz := wz - cz * chunk_size
	
	return chunk.is_air(lx, ly, lz)
	
	
	
func _on_chunk_mesh_updated(chunk, triangles) -> void:
	var key = chunk.key
	var old := 0
	
	if _chunk_triangles.has(key):
		old = _chunk_triangles[key]
		
	_chunk_triangles[key] = triangles
	triangles_total += triangles - old



	
func _on_border_update_requested(neighbor_key: Vector2i) -> void:
	if _chunks.has(neighbor_key):
		_chunks[neighbor_key].build_mesh()
		
		
func add_block_world(world_coords: Vector3i) -> void:
	var wx = world_coords.x
	var wy = world_coords.y
	var wz = world_coords.z
	
	if wx < 0 or wy < 0 or wz < 0 or wx >= chunk_size * world_size or wy >= chunk_size or wz >= chunk_size * world_size:
		return
		
	# check chunk coordinates
	var cx = wx / chunk_size
	var cz = wz / chunk_size
	var key := Vector2i(cx, cz)
	
	if _chunks.has(key):
		_chunks[key].add_block(world_coords)


func delete_block_world(world_coords: Vector3i) -> void:
	var wx = world_coords.x
	var wy = world_coords.y
	var wz = world_coords.z
	
	if wx < 0 or wy < 0 or wz < 0 or wx >= chunk_size * world_size or wy >= chunk_size or wz >= chunk_size * world_size:
		return
		
	# check chunk coordinates
	var cx = wx / chunk_size
	var cz = wz / chunk_size
	var key := Vector2i(cx, cz)
	
	if _chunks.has(key):
		_chunks[key].delete_block(world_coords)
"

[sub_resource type="StandardMaterial3D" id="StandardMaterial3D_bx44p"]
transparency = 1
shading_mode = 0
albedo_texture = ExtResource("3_1ynvo")
albedo_texture_force_srgb = true
uv1_offset = Vector3(0.5, 0.5, 0.5)
uv1_triplanar = true

[sub_resource type="BoxMesh" id="BoxMesh_v5fa5"]
material = SubResource("StandardMaterial3D_bx44p")
size = Vector3(1.02, 1.02, 1.02)

[sub_resource type="CapsuleShape3D" id="CapsuleShape3D_w4k74"]
radius = 0.3
height = 1.6

[sub_resource type="Gradient" id="Gradient_8ls5l"]
offsets = PackedFloat32Array(0.463127, 0.469027)
colors = PackedColorArray(1, 1, 1, 1, 1, 1, 1, 0)

[sub_resource type="GradientTexture2D" id="GradientTexture2D_1ynvo"]
gradient = SubResource("Gradient_8ls5l")
fill = 1
fill_from = Vector2(0.5, 0.5)

[node name="VoxelEngine" type="Node3D"]
script = SubResource("GDScript_w4k74")

[node name="WorldEnvironment" type="WorldEnvironment" parent="."]
environment = SubResource("Environment_8ls5l")

[node name="DirectionalLight3D" type="DirectionalLight3D" parent="."]
transform = Transform3D(-0.866023, -0.433016, 0.250001, 0, 0.499998, 0.866027, -0.500003, 0.749999, -0.43301, 0, 0, 0)
shadow_enabled = true

[node name="ChunkManager" type="Node" parent="."]
script = SubResource("GDScript_8ls5l")
world_size = 8

[node name="CubeSelection" type="Node3D" parent="."]

[node name="MeshInstance3D" type="MeshInstance3D" parent="CubeSelection"]
mesh = SubResource("BoxMesh_v5fa5")

[node name="Player" type="CharacterBody3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 8, 50, 8)
script = ExtResource("3_8ls5l")

[node name="Camera3D" type="Camera3D" parent="Player"]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.5, 0)

[node name="RayCast3D" type="RayCast3D" parent="Player/Camera3D"]
target_position = Vector3(0, 0, -5)

[node name="CollisionShape3D" type="CollisionShape3D" parent="Player"]
shape = SubResource("CapsuleShape3D_w4k74")

[node name="FPSLabel" type="Label" parent="."]
anchors_preset = 1
anchor_left = 1.0
anchor_right = 1.0
offset_left = -40.0
offset_bottom = 23.0
grow_horizontal = 0
theme_override_font_sizes/font_size = 60
text = "FPS : "

[node name="TrianglesLabel" type="Label" parent="."]
anchors_preset = 1
anchor_left = 1.0
anchor_right = 1.0
offset_left = -311.0
offset_top = 100.0
offset_bottom = 183.0
grow_horizontal = 0
theme_override_font_sizes/font_size = 60
text = "Triangles : "

[node name="Pointer" type="TextureRect" parent="."]
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -20.0
offset_top = -20.0
offset_right = 20.0
offset_bottom = 20.0
grow_horizontal = 2
grow_vertical = 2
scale = Vector2(0.2, 0.2)
pivot_offset = Vector2(32, 32)
texture = SubResource("GradientTexture2D_1ynvo")
